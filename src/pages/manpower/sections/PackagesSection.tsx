import type { SiteContent } from "@/shared/schemas/content.schema"; import { SectionShell } from "@/shared/layout/SectionShell"; import { ServiceList } from "@/features/services/ServiceList"; import { mapPackageContentToServicePackage } from "@/entities/service/mappers"; export function PackagesSection({ content }: { content: SiteContent }) { const items = content.manpower.packages.map((item,index)=>mapPackageContentToServicePackage(item,"manpower",`manpower-${index+1}`)); return <SectionShell innerClassName="pt-0"><ServiceList items={items} convertLabel={content.common.convertToBdt} hideLabel={content.common.hideBdt} /></SectionShell>; }
