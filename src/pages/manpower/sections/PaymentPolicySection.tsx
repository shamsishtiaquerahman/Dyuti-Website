import type { SiteContent } from "@/shared/schemas/content.schema"; import { SectionShell } from "@/shared/layout/SectionShell"; import { Card } from "@/shared/ui/Card"; import { SectionTitle } from "@/shared/ui/SectionTitle"; export function PaymentPolicySection({ content }: { content: SiteContent }) { return <SectionShell><SectionTitle title={content.manpower.paymentTitle} description={content.manpower.paymentText} /><Card className="mt-8"><div className="space-y-3 text-slate-700 dark:text-slate-300">{content.manpower.policies.map((item)=><p key={item}>{item}</p>)}</div></Card></SectionShell>; }
