import type { SiteContent } from "@/shared/schemas/content.schema"; import { SectionShell } from "@/shared/layout/SectionShell"; import { SectionTitle } from "@/shared/ui/SectionTitle"; import { FaqList } from "@/features/faq/FaqList"; import { mapFaqContentToEntity } from "@/entities/faq/mappers"; export function FaqSection({ content }: { content: SiteContent }) { const items = content.contact.faqs.map((item,index)=>mapFaqContentToEntity(item,`contact-faq-${index+1}`)); return <SectionShell className="border-y border-slate-200 bg-slate-50 dark:border-white/10 dark:bg-white/5"><SectionTitle title={content.contact.faqTitle} description={content.contact.faqText} /><div className="mt-10"><FaqList items={items} /></div></SectionShell>; }
