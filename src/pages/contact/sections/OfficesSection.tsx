import type { SiteContent } from "@/shared/schemas/content.schema"; import { SectionShell } from "@/shared/layout/SectionShell"; import { OfficeCard } from "@/features/offices/OfficeCard"; import { mapOfficeContentToEntity } from "@/entities/office/mappers"; export function OfficesSection({ content }: { content: SiteContent }) { const offices = content.common.offices.map((office,index)=>mapOfficeContentToEntity(office,`office-${index+1}`)); return <SectionShell innerClassName="pt-0"><div className="grid gap-6 lg:grid-cols-2">{offices.map((office)=><OfficeCard key={office.id} office={office} labels={{address:content.common.address,zipCode:content.common.zipCode,email:content.common.email,messageOffice:content.common.messageOffice}} />)}</div></SectionShell>; }
