import type { SiteContent } from "@/shared/schemas/content.schema"; import { DocumentList } from "@/features/documents/DocumentList"; import { mapDocumentLabelToEntity } from "@/entities/document-rule/mappers"; export function StudentDocumentsSection({ content }: { content: SiteContent }) { const items = content.student.docs.map((item,index)=>mapDocumentLabelToEntity(item,`student-documents-page-${index+1}`,"student")); return <div className="rounded-[30px] border border-slate-200 bg-white p-7 shadow-sm dark:border-white/10 dark:bg-white/5"><h3 className="text-2xl font-semibold text-slate-900 dark:text-white">{content.student.docsTitle}</h3><div className="mt-6"><DocumentList items={items} /></div></div>; }
