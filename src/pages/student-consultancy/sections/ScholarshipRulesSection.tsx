import type { SiteContent } from "@/shared/schemas/content.schema"; import { SectionShell } from "@/shared/layout/SectionShell"; import { Card } from "@/shared/ui/Card"; export function ScholarshipRulesSection({ content }: { content: SiteContent }) { return <SectionShell><div className="grid gap-6 lg:grid-cols-2"><Card><h3 className="text-xl font-semibold text-slate-900 dark:text-white">{content.student.importantConditions}</h3><div className="mt-5 space-y-3 text-slate-700 dark:text-slate-300">{content.student.policies.map((item)=><p key={item}>{item}</p>)}</div></Card><Card><h3 className="text-xl font-semibold text-slate-900 dark:text-white">{content.student.scholarshipRules}</h3><div className="mt-5 space-y-3 text-slate-700 dark:text-slate-300"><p>{content.student.scholarshipRulesText}</p>{content.student.scholarshipRulesList.map((item)=><p key={item}>{item}</p>)}</div></Card></div></SectionShell>; }
