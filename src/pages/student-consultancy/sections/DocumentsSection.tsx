import type { SiteContent } from "@/shared/schemas/content.schema"; import { SectionShell } from "@/shared/layout/SectionShell"; import { SectionTitle } from "@/shared/ui/SectionTitle"; import { DocumentList } from "@/features/documents/DocumentList"; import { mapDocumentLabelToEntity } from "@/entities/document-rule/mappers"; export function DocumentsSection({ content }: { content: SiteContent }) { const items = content.student.docs.map((item,index)=>mapDocumentLabelToEntity(item,`student-doc-${index+1}`,"student")); return <SectionShell className="border-y border-slate-200 bg-slate-50 dark:border-white/10 dark:bg-white/5"><SectionTitle title={content.student.docsTitle} /><div className="mt-8"><DocumentList items={items} /></div></SectionShell>; }
