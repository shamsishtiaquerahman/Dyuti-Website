import type { SiteContent } from "@/shared/schemas/content.schema"; import { SectionShell } from "@/shared/layout/SectionShell"; import { DocumentList } from "@/features/documents/DocumentList"; import { mapDocumentLabelToEntity } from "@/entities/document-rule/mappers"; export function ServicesSection({ content }: { content: SiteContent }) { const items = content.student.services.map((item,index)=>mapDocumentLabelToEntity(item,`student-service-${index+1}`,"student")); return <SectionShell innerClassName="pt-0"><DocumentList items={items} /></SectionShell>; }
