import type { SiteContent } from "@/shared/schemas/content.schema"; import { SectionShell } from "@/shared/layout/SectionShell"; import { SectionTitle } from "@/shared/ui/SectionTitle"; import { Card } from "@/shared/ui/Card"; import { PriceWithBdt } from "@/features/pricing/PriceWithBdt"; export function TuitionPackagesSection({ content }: { content: SiteContent }) { return <SectionShell className="border-y border-slate-200 bg-slate-50 dark:border-white/10 dark:bg-white/5"><SectionTitle title={content.student.tuitionTitle} description={content.student.tuitionText} /><div className="mt-10 grid gap-6 lg:grid-cols-3">{content.student.waiverPackages.map((pkg)=><Card key={pkg.name}><p className="text-sm font-semibold uppercase tracking-[0.24em] text-dyutiTeal">{pkg.name}</p><div className="mt-4"><PriceWithBdt price={pkg.price} bdt={pkg.bdt} convertLabel={content.common.convertToBdt} hideLabel={content.common.hideBdt} /></div><p className="mt-4 text-slate-700 dark:text-slate-300">{pkg.firstPayment}</p><p className="mt-2 text-slate-700 dark:text-slate-300">{pkg.secondPayment}</p></Card>)}</div></SectionShell>; }
