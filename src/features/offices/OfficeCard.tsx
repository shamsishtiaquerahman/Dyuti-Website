import type { OfficeEntity } from "@/entities/office/model";
import { Card } from "@/shared/ui/Card";
export function OfficeCard({ office, labels }: { office: OfficeEntity; labels: { address: string; zipCode: string; email: string; messageOffice: string } }) {
  return <Card className="h-full"><p className="text-sm font-semibold uppercase tracking-[0.24em] text-dyutiTeal">{office.title}</p><h3 className="mt-4 text-2xl font-bold text-slate-900 dark:text-white">{office.city}</h3><div className="mt-6 space-y-4 text-slate-700 dark:text-slate-300"><p><span className="font-semibold text-slate-900 dark:text-white">{labels.address}:</span> {office.address}</p><p><span className="font-semibold text-slate-900 dark:text-white">{labels.zipCode}:</span> {office.zip}</p><p><span className="font-semibold text-slate-900 dark:text-white">{office.whatsappLabel}:</span> {office.whatsapp}</p><p><span className="font-semibold text-slate-900 dark:text-white">{labels.email}:</span> info@dyutigroup.net</p></div><a href={office.waLink} className="mt-7 inline-flex rounded-2xl border border-slate-200 bg-white px-5 py-3 text-sm font-semibold text-slate-950" rel="noreferrer">{labels.messageOffice} {office.title}</a></Card>;
}
