# DYUTI React Website v13

This version restores the visible and working language toggles:
- English / Bengali / Arabic buttons are visible
- language switching works
- dark / light mode works
- BMET card is included in both manpower packages
- favicon is configured
- React + Tailwind + TypeScript + Vite

Run:
```bash
npm install
npm run dev
```

- Required documents updated: removed 'Clear scanned copy of passport' and changed the photo wording to 'White background passport-size photo softcopy'
